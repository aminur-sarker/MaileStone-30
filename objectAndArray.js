
// we can use the Esc bellow button as like: ''    ""

const num = [10,20,30];
const student = {
    name : 'Aminur',
    age : 25
}
const math = `The sum of ${num[0]} and ${student.name} is ${student.name+num[0]}`
console.log(math)